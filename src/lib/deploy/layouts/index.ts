/**
 * Layout system barrel export.
 * Provides layout configs and CSS generation for 20 distinct site layouts.
 */

export { getLayoutConfig, availableLayouts, type LayoutConfig } from './layout-definitions';
export { getLayoutStyles } from './layout-css';
